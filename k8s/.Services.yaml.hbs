apiVersion: v1
kind: ServiceList
items:
  - apiVersion: v1
    kind: Service
    metadata:
      name: {{dashCase name}}
    spec:
      selector:
        app: {{dashCase name}}
      type: NodePort
      ports:
        - port: 80
          targetPort: 80
